{
  "name": "deliverx",
  "version": "1.0.0",
  "description": "DeliverX - Modern delivery service website",
  "main": "index.js",
  "scripts": {
    "build": "sass scss/styles.scss css/styles.css --no-source-map",
    "build:compressed": "sass scss/styles.scss css/styles.css --style=compressed --no-source-map",
    "watch": "sass --watch scss/styles.scss:css/styles.css",
    "dev": "sass --watch scss/styles.scss:css/styles.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["delivery", "logistics", "website", "bootstrap", "gsap"],
  "author": "DeliverX Team",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.69.0"
  }
}
